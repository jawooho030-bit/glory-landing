window.addEventListener("scroll", () => {
  const btn = document.getElementById("floatingLine");

  if (window.scrollY > 300) {
    btn.style.opacity = "1";
  } else {
    btn.style.opacity = "1";
  }
});
